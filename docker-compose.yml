version: "3.0"
services: 
  cssq1:
    build: .
    ports:
      - "8080:8080"
    links:
      - mongo

  mongo:
      image: mongo
      volumes:
        - /data/mongodb/db:/data/db
      ports:
        - "27017:27017"